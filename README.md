# dotnet-dd-admissions-exmp
This is an example of a dotnet hello world app that has been Instrumentated via datadog Cluster Agent Admission Controller so that users do not have to do code level changes.

